Why is it called Regexp instead of Regex? Regular Exponential, lol I like it.

`message` parameter in __call__? Will that ever be used?

`match = self.regex.match(field.data or "")` that passes `None`. We'll sort this None behaviour. Read module.md
